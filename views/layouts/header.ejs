<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm fixed-top">
  <div class="container">
    <a class="navbar-brand fw-bold text-primary" href="/">Smart Waste Management</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">

        <li class="nav-item">
          <a class="nav-link" href="/">Home</a>
        </li>

        <% if (currentAdmin) { %>
          <!-- ✅ Admin logged in -->
          <li class="nav-item"><a class="nav-link" href="/admin/dashboard">Dashboard</a></li>
          <li class="nav-item">
            <form action="/admin/logout" method="POST" class="d-inline">
              <button class="btn btn-outline-danger btn-sm">
                <i class="bi bi-box-arrow-right"></i> Logout
              </button>
            </form>
          </li>

        <% } else if (currentEmployee) { %>
          <!-- ✅ Employee logged in -->
          <li class="nav-item"><a class="nav-link" href="/workers/employee/mytask">My Tasks</a></li>
          <li class="nav-item">
            <form action="/workers/employee/logout" method="POST" class="d-inline">
              <button class="btn btn-outline-danger btn-sm">
                <i class="bi bi-box-arrow-right"></i> Logout
              </button>
            </form>
          </li>

        <% } else if (currentUser) { %>
          <!-- ✅ User logged in -->
          <li class="nav-item"><a class="nav-link" href="/complaints/user/add">Raise Complaint</a></li>
          <li class="nav-item"><a class="nav-link" href="/history">All Complaints</a></li>
          <li class="nav-item"><a class="nav-link" href="/complaints/user/mycomplaints">My Complaints</a></li>
          <li class="nav-item">
            <form action="/users/logout" method="POST" class="d-inline">
              <button class="btn btn-outline-danger btn-sm">Logout</button>
            </form>
          </li>

        <% } else { %>
          <!-- ✅ Guest (no one logged in) -->
          <li class="nav-item"><a class="nav-link" href="/users/login">Login</a></li>
          <li class="nav-item"><a class="nav-link" href="/users/signup">Signup</a></li>
        <% } %>

      </ul>
    </div>
  </div>
</nav>
